template<typename T>
class Vector{
private:
    size_t n;
    bool is_initialize=false; //for check before using func
public:
    Vector(/* args */);
    ~Vector();
    //initialize by const 
    //initialize by random values in [a, b]
    //export to file
    //import from file
    //min()(with indx) max()(with indx) mean() sum() eclid_metr()
    //scalr_mult_vectors() 
};